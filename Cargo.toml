[workspace.package]
authors = ["{{ authors }}",]
categories = [ ]
description = "{{ description }}"
edition = "2021"
homepage = "https://{{ git }}/{{ project-name }}/wiki"
keywords = [ ]
license = "Apache-2.0"
readme = "README.md"
repository = "https://{{ git }}/{{ project-name }}.git"
version = "0.0.0"

[workspace.dependencies]

[workspace]
default-members = [
    "{{ project-name }}"
]

exclude = [

]

members = [
    "{{ project-name }}",
]

resolver = "2"

[profile.dev]
opt-level = 0
debug = true
debug-assertions = true
overflow-checks = true
lto = false
panic = 'unwind'
incremental = true
codegen-units = 256
rpath = false

[profile.release]
opt-level = 3
debug = false
debug-assertions = false
overflow-checks = false
lto = false
panic = 'unwind'
incremental = false
codegen-units = 16
rpath = false
